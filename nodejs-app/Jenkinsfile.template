pipeline {
  agent any
  options { 
    buildDiscarder(logRotator(numToKeepStr: '2'))
  }
  stage('Test') {
    steps {
      container('nodejs') {
        echo 'Hello World!'   
        sh 'node --version'
      }
    }
  }
}
